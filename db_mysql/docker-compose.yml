  services:
    mysql:
      image: mysql:8.0
      container_name: mysql_local
      restart: unless-stopped
      env_file:
        - .env
      ports:
        - "3306:3306"
      volumes:
        - db_mysql:/var/lib/mysql

  volumes:
    db_mysql: